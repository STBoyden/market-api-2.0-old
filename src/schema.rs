table! {
    items (item_id) {
        item_id -> Integer,
        name -> Varchar,
        price -> Integer,
        stock -> Integer,
        owner -> Varchar,
        posted_timestamp -> Datetime,
    }
}
